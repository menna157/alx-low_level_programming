#include "main.h"
#include <string.h>

/**
 * _strlen - this is a function that returns the length of a string.
 *
 * @s: function input.
 *
 * Return: the length of a string.
 */

int _strlen(char *s)
{
	return (strlen(s));
}
